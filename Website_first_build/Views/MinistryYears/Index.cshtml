@model IEnumerable<Website_first_build.Models.MinistryYear>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayoutPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.YearName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.YearDes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Category.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.YearName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.YearDes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Category.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.YearID }) |
            @Html.ActionLink("Details", "Details", new { id=item.YearID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.YearID })
        </td>
    </tr>
}

</table>
